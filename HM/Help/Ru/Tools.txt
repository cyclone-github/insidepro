
$HexToPlain		конвертирует строки из формата $HEX[...] в обычный текст
Base64ToHex		конвертирует строки из формата Base64 в формат Hex
Base64ToPlain		конвертирует строки из формата Base64 в обычный текст
BinToHex		конвертирует бинарный файл в строки формата Hex
CalculateChecksum	подсчитывает контрольную сумму файла
CalculateRatio		подсчитывает соотношение строк для двух файлов
CalculateTime		подсчитывает время перебора маски
CharToUnicode		конвертирует текстовый файл в формат Unicode
CombineLines		комбинирует строки из двух файлов
ConvertToForum		конвертирует выходной файл в формат "хэш:[color=blue]пароль[/color]"
ConvertToLower		конвертирует строки в нижний регистр
ConvertToUpper		конвертирует строки в верхний регистр
CountLines		подсчитывает количество строк
EnumerateLines		нумерует строки
ExtractColumn		извлекает заданный столбец
ExtractLines		извлекает заданный диапазон строк
ExtractLinesByLen	извлекает строки заданной длины
ExtractPasswords	извлекает пароли из выходного файла
ExtractRepeates		извлекает повторы из строк
FindByMask		ищет пароли по маске
FindDamagedHashes	ищет частично совпадающие хэши
FindString		ищет строки
GenerateHash		генерирует хэш для заданного пароля
GenerateHashes		генерирует хэши для всех строк в файле
GeneratePassword	генерирует случайный пароль
GenerateTestHashes	генерирует тестовые хэши
GetMasks		формирует маски для каждого пароля
GetMaxLen		возвращает максимальную длину строки в файле
GetTopLines		возвращает строки, отсортированные по частоте упоминания
HexToBase64		конвертирует строки из формата Hex в формат Base64
HexToBin		конвертирует строки из формата Hex в бинарный файл
HexToMix		генерирует массив хэшей формата Hex со всеми возможными комбинациями байта (или полубайта)
HexToPlain		конвертирует строки из формата Hex в обычный текст
InsertCharToPos		вставляет символ в заданную позицию каждой строки
IsCharset		оставляет только строки, состоящие из заданного набора символов
IsCharsetInPos		оставляет только строки, где определенный символ состоит из заданного набора символов
IsDouble		оставляет только строки, состоящие из двух одинаковых паролей
IsEmail			оставляет только строки формата почтовых ящиков
IsLower			оставляет только строки в нижнем регистре
IsOutfile		оставляет только строки формата "хэш:пароль"
IsRules			оставляет только правила для HM
IsUpper			оставляет только строки в верхнем регистре
MergePairs		объединяет имена пользователей с паролями
MergeToOutfile		объединяет списки хэшей и паролей в единый выходной файл
ParseDump		парсит SQL-дамп
PlainTo$Hex		конвертирует строки в формат $HEX[...]
PlainToBase64		конвертирует строки в формат Base64
PlainToHex		конвертирует строки в формат Hex
RemoveChar		удаляет символ
RemoveCharInPos		удаляет символ в заданной позиции каждой строки
RemoveColumn		удаляет заданный столбец, разделенный двоеточиями
RemoveDuplicates	удаляет из первого файла те строки, которые имеются во втором файле
RemoveEmptyLines	удаляет пустые строки
RemoveHashes		удаляет хэши с найденными паролями
RemoveLeft		удаляет часть строки слева
RemoveLines		удаляет строки, содержащие заданный символ
RemoveOutsideChar	удаляет крайние символы из строк
RemoveRight		удаляет часть строки справа
RemoveSpecialChar	удаляет служебные символы с ASCII-кодами ниже 32
RenameByLines		переименовывает файл по количеству строк
ReplaceCharByIndex	заменяет символ по номеру
ReplaceCharInPos	заменяет символ в заданной позиции каждой строки
ReplaceString		заменяет одну строку на другую
ReverseLines		переворачивает строки
SortLines		сортирует строки по возрастанию или убыванию
SortLinesByLen		сортирует строки по возрастанию длины
SplitByFiles		разделает файл на заданное число файлов
SplitByFirstChar	разделает файл по первому символу строк
SplitByLen		разделает файл по длине строк
SplitByLines		разделает файл по заданному количеству строк
SplitOutfile		разделяет выходной файл на хэши и пароли
SwapColumns		меняет местами столбцы, разделенные двоеточиями
SwapHex			меняет местами байты в строках формата Hex
ToggleCharCase		генерирует пароли со всеми возможными регистрами символов
TrimLines		удаляет пробелы и знаки табуляции в начале и в конце строк
TruncateLines		обрезает строки до заданной длины
UnicodeToChar		конвертирует файл из формата Unicode в текстовый формат
UseRules		применяет правила HM к текстовому файлу
VerifyOutfile		верифицирует хэши и пароли для некоторых алгоритмов
